#include <stdio.h>
#include<conio.h>
#include<string.h>

int main() { 
      int a,b,c,d;



  
    a = 48;
    while (a <= 57)
    {
        printf("%d = %c\n", a, a);
        a++;
    }
     
for(b=65;b<=90;b++)
  {
	  int b;
    b = 65;
    while (b <= 90)
    {
        printf("%d = %c\n", b, b);
        b++;
    }}
for(c=65;c<=90;c++)
{

    c= 65;
    while (c <= 90)
    {
        printf("%d = %c\n", c, c);
        c++;
    }
    return 0;
} }
